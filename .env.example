# Qyrova environment — copy this file to `.env` and fill in your values.
#
# Leave these BLANK to run Qyrova in local-only mode (localStorage, $0, no backend).
# Set them to turn on the real multi-tenant cloud backend (Supabase).
#
# Find them in: Supabase Dashboard → your project → Settings → API
#   - Project URL          -> VITE_SUPABASE_URL
#   - Project API keys: anon/public -> VITE_SUPABASE_ANON_KEY
#
# The anon key is safe to expose in the browser; Row-Level Security protects data.
# NEVER put the service_role / secret key here.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
